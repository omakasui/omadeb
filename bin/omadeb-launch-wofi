#!/bin/bash

if pgrep -x wofi > /dev/null; then
  pkill wofi
fi

if [ $XDG_SESSION_TYPE = "x11" ]; then
    exec wofi --normal-window --cache-file=/dev/null $@
else
    exec wofi --cache-file=/dev/null $@
fi