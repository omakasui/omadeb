#!/bin/bash

STATE_FILE=~/.local/state/omadeb/toggles/suspend-off

if [[ -f $STATE_FILE ]]; then
  rm -f $STATE_FILE
  omadeb-notify -u low "Suspend" "󰒲   Suspend now available in system menu"
else
  mkdir -p "$(dirname $STATE_FILE)"
  touch $STATE_FILE
  omadeb-notify -u low "Suspend" "󰒲   Suspend removed from system menu"
fi